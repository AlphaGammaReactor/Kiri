[project]
name = "kiri-backend"
version = "0.1.0"
description = "Kiri PARL-MAVS Research Platform — FastAPI Backend"
requires-python = ">=3.11"
readme = "README.md"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
