server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # ── SPA Catch-All ──
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ── API Reverse Proxy ──
    # Proxy /api requests to the backend Railway service
    # BACKEND_URL is substituted by envsubst on container start
    location /api/ {
        proxy_pass ${BACKEND_URL}/api/;
        proxy_set_header Host kiri-production.up.railway.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for long-running bio-computation requests
        proxy_read_timeout 300s;
        proxy_connect_timeout 10s;
        proxy_send_timeout 300s;
    }

    # ── Static Asset Caching ──
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ── Gzip Compression ──
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
    gzip_min_length 256;
}
